Move `vbd_dat_rigid_soft` to the multiphysics examples and extend it with a second cloth sheet and three resting rigid bodies. Its `python -m newton.examples vbd_dat_rigid_soft` command is unchanged.
