Deprecate `newton.actuators.ClampingDCMotor.corner_velocity`. It is now derived from the live parameters on access rather than stored at construction, so it no longer goes stale when a parameter is retuned. Compute it as `velocity_limit * (1 + max_motor_effort / saturation_effort)` if you need the value.
