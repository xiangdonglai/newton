Keep at least 5% of the primitive gap on each side of DAT division planes, in addition to the scale-aware epsilon band when it fits. Leave soft-self pairs with no certifiable strict separator unconstrained by DAT on that pass so contact forces can attempt recovery, instead of freezing them indefinitely. Rigid-soft BVH pairs still freeze when no separator is found; initialize simulations with separated geometry.
